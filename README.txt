
# osgi properties and their defaults

# date format used in logging (default = no date)
com.eriklievaart.osgi.logging.date=yyyy-MM-dd HH:mm:ss
# config file used to set levels (default = FINEST)
com.eriklievaart.toolkit.logging.config.file=
